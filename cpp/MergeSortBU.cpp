#include <iostream>
#include <algorithm>
#include <string>
#include "sortTestHelper.h"
using namespace std;
template<typename T>
void mergeSortBu(T arr[], int n){
//思路整理：
// 将数组拆分为单个元素一组，然后每次相邻的一组进行比较并合并，依次向上累加；
// 最外层循环：要merge的次数；第一次是两个元素进行merge,第二次是四个，依次往上累加，直至最后依次整个数组的两部分进行累加
// 内层循坏：这是个merge的循环，每次要merge的数组，在每一轮的循环当中
}

int main(){

	return 0;
}